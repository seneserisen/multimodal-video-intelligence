# ADR 0005: No default cookie export

Cookies are high-value credentials. Using normal authenticated browser playback avoids copying them and materially reduces exposure, so cookie export is not a default acquisition method.
