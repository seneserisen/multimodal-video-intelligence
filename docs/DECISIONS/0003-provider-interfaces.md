# ADR 0003: Provider interfaces

AI and extraction providers stay behind typed interfaces. This preserves local and fake operation, makes cost/privacy explicit, and prevents vendor-specific models from leaking into fusion.
