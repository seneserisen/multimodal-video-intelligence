# ADR 0004: Tab capture as fallback

Direct authorised acquisition is simpler and higher fidelity. Authenticated tab capture is the practical fallback when direct methods fail; server-side scraping is brittle and can cross access boundaries.
